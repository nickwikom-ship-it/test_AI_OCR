import streamlit as st
import cv2
import pytesseract
import numpy as np
from PIL import Image

# --- การตั้งค่าหน้าเว็บ ---
st.set_page_config(
    page_title="AI Multi-Lang OCR",
    page_icon="🔍",
    layout="wide"
)

# Custom CSS เพื่อให้ UI ดูสะอาดตา
st.markdown("""
    <style>
    .main {
        background-color: #f5f5f5;
    }
    .stTextArea textarea {
        font-size: 18px !important;
    }
    </style>
    """, unsafe_allow_index=True)

st.title("📸 AI Multi-Language Text Scanner")
st.write("ระบบดึงข้อความจากภาพ (รองรับ ไทย, อังกฤษ, จีน) - *เวอร์ชันปรับปรุงความเร็ว*")

# --- ส่วน Sidebar ---
st.sidebar.header("⚙️ การตั้งค่า")

# เลือกภาษา (รวมรหัส Tesseract ไว้ในตัวเลือก)
lang_options = {
    "ไทย + English": "tha+eng",
    "English Only": "eng",
    "ภาษาจีน (ตัวย่อ)": "chi_sim",
    "ภาษาจีน (ตัวเต็ม)": "chi_tra",
    "ไทย + จีน": "tha+chi_sim"
}
selected_option = st.sidebar.selectbox("เลือกภาษาที่แสดงในภาพ:", list(lang_options.keys()))
selected_lang = lang_options[selected_option]

# ตัวเลือกการปรับภาพ
auto_sharpen = st.sidebar.checkbox("เพิ่มความคมชัดอัตโนมัติ", value=True)

# อัปโหลดไฟล์
uploaded_file = st.sidebar.file_uploader("อัปโหลดรูปภาพ (JPG, PNG)...", type=["jpg", "jpeg", "png"])

# --- ส่วนประมวลผลหลัก ---
if uploaded_file is not None:
    # 1. โหลดภาพ
    image = Image.open(uploaded_file)
    img = np.array(image.convert('RGB'))
    
    col1, col2 = st.columns(2)

    with col1:
        st.subheader("🖼️ ภาพต้นฉบับ")
        st.image(image, use_container_width=True)

    # 2. กระบวนการ Image Enhancement (เน้นความเร็ว)
    with st.spinner('กำลังประมวลผลภาพ...'):
        # แปลงเป็นขาวดำ
        gray = cv2.cvtColor(img, cv2.COLOR_RGB2GRAY)
        
        if auto_sharpen:
            # เพิ่ม Contrast และความคมชัดแบบรวดเร็ว (Fast Mode)
            # ใช้ Otsu's Threshold เพื่อแยก Background/Foreground ทันที
            _, processed_img = cv2.threshold(gray, 0, 255, cv2.THRESH_BINARY + cv2.THRESH_OTSU)
        else:
            processed_img = gray

    with col2:
        st.subheader("✨ ภาพที่ AI ใช้ประมวลผล")
        st.image(processed_img, use_container_width=True, channels="GRAY")

    # 3. กระบวนการ OCR (เน้นความเร็ว)
    st.divider()
    with st.spinner(f'AI กำลังอ่านข้อความ ({selected_option})...'):
        try:
            # --oem 1: ใช้ Neural nets LSTM engine (แม่นยำและเร็ว)
            # --psm 6: ถือว่าภาพเป็นข้อความก้อนเดียว (เหมาะกับไฟล์ขนาดเล็ก/เอกสาร)
            custom_config = r'--oem 1 --psm 6'
            
            # รัน OCR
            text = pytesseract.image_to_string(processed_img, lang=selected_lang, config=custom_config)
            
            if text.strip():
                st.subheader("📄 ข้อความที่สแกนได้:")
                result_area = st.text_area("", text, height=400)
                
                # ปุ่มดาวน์โหลด
                st.download_button(
                    label="📥 ดาวน์โหลดไฟล์ข้อความ",
                    data=text.encode('utf-8'),
                    file_name="scanned_result.txt",
                    mime="text/plain"
                )
            else:
                st.warning("⚠️ AI ไม่พบข้อความในรูปภาพ ลองเปิด 'เพิ่มความคมชัดอัตโนมัติ' หรือตรวจสอบว่าเลือกภาษาถูกต้อง")
        
        except Exception as e:
            st.error(f"เกิดข้อผิดพลาดในการประมวลผล: {e}")
            st.info("คำแนะนำ: ตรวจสอบว่าได้ติดตั้งไฟล์ภาษาในระบบ (packages.txt) เรียบร้อยแล้ว")

else:
    # หน้าจอตอนยังไม่ได้อัปโหลดไฟล์
    st.info("💡 กรุณาอัปโหลดไฟล์ภาพจากแถบด้านข้าง เพื่อเริ่มการสแกน")
    
    # แสดงตัวอย่างขั้นตอนการทำงาน
    st.markdown("""
    ### วิธีใช้งาน:
    1. เลือก **ภาษา** ให้ตรงกับเอกสารของคุณ
    2. อัปโหลดรูปภาพ (รองรับขนาดไฟล์เล็กและใหญ่)
    3. รอ AI ประมวลผลและคัดลอกข้อความไปใช้งานได้ทันที
    """)
